# Pull CUDA-enabled torch wheels (the `+cu128` local version outranks the
# plain CPU wheel on PyPI, so pip picks the CUDA build even with `torch>=2.0`
# in requirements-common.txt). Works for Ampere (30xx), Ada (40xx), Blackwell (50xx+).
--extra-index-url https://download.pytorch.org/whl/cu128

-r requirements-common.txt

# NVIDIA CUDA
onnxruntime-gpu
jax[cuda12]

# Note: do NOT install onnxruntime (CPU) alongside onnxruntime-gpu — they conflict.
# If you previously installed the CPU version, run:
#   pip install onnxruntime-gpu --force-reinstall

# Note: llama-cpp-python ships as a CPU-only wheel. The ggml runner auto-rebuilds
# it with Vulkan (preferred) or CUDA on first run.
# Linux: apt install libvulkan-dev glslc  (or nvidia-cuda-toolkit ≥12.8 for Blackwell)
# Windows: install Vulkan SDK (https://vulkan.lunarg.com) or CUDA Toolkit
