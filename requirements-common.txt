# Core
torch>=2.0
numpy

# Models
transformers>=4.30
torchvision
safetensors
huggingface_hub

# ONNX model format
onnx
onnxscript  # required by torch.onnx.export(dynamo=True)

# GGML backends
llama-cpp-python
faster-whisper
gguf

# MAX (Modular) — MLIR-based inference engine, Linux or macOS (Apple Silicon).
# Not available on Windows; harmless to list — pip will simply not find it there.
max
