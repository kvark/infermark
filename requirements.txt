# Core
torch>=2.0
numpy

# Models
transformers>=4.30
torchvision
safetensors
huggingface_hub

# ONNX Runtime — pick ONE based on your GPU:
onnxruntime                  # CPU only
# onnxruntime-gpu            # NVIDIA CUDA
# onnxruntime-rocm           # AMD ROCm
onnx

# JAX — pick ONE based on your GPU:
jax                          # CPU only
# jax[cuda12]                # NVIDIA CUDA 12
# jax-metal                  # Apple Metal
jaxlib

# GGML backends
# Default pip install builds CPU-only. Rebuild with GPU flags:
#   CMAKE_ARGS="-DGGML_CUDA=ON"   pip install llama-cpp-python --force-reinstall --no-cache-dir
#   CMAKE_ARGS="-DGGML_VULKAN=ON" pip install llama-cpp-python --force-reinstall --no-cache-dir
#   CMAKE_ARGS="-DGGML_METAL=ON"  pip install llama-cpp-python --force-reinstall --no-cache-dir
llama-cpp-python
faster-whisper
